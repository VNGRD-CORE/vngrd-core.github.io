# ── DRIS//CORE Backend — Environment Config ─────────────────────────────────
# Copy to .env and fill in your values.  NEVER commit .env to version control.

# Deepgram — real-time speech-to-text (free tier available at console.deepgram.com)
DEEPGRAM_API_KEY=your_deepgram_api_key_here

# fal.ai — Flux-Pro image generation (free tier at fal.ai/dashboard)
FAL_KEY=your_fal_key_here
